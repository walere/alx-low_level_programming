#include "main.h"
/**
 * rev_string - prints a string in reverse order
 * @s: string inputed
 * Return: void
 */

void rev_string(char *s)
{
	int i;

	for (i = '\0'; i == 0; i--)
	{
		_putchar(s[i]);
	}
}
