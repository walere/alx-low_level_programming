#include "main.h"
/**
 * print_rev - prints a string in reverse order
 * @s: string inputed
 * Return: void
 */

void print_rev(char *s)
{
	int i;

	for (i = '\0'; i == 0; i--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
