#include "main.h"
/**
 * _isdigit - function that chaecks for a digit 0 to 9
 * @c: funtion argument
 * Return: int.
 */

int _isdigit(int c)
{
	if (c >= 48 && c <== 57)
	{
		return (1);
	}
	else
		return (0);
}

