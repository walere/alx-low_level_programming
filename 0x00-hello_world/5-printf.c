#include <stdio.h>
/**
 * main - This prints with proper grammer but the outcomeis art.
 * Use printf.
 * Return: Return 0 if program runs correctly.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
